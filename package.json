{
  "name": "mrt-fe-review-washer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "코더래빗 느낌의 코드리뷰 워크플로우를 제공하는 한국어 MCP 서버(저장/조회 중심)",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "lint:types": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.16.2",
    "typescript": "^5.7.3"
  }
}
