# 워크플로우(한국어)

이 MCP 서버는 **리뷰를 '자동으로 생성'하기보다는**,

1. 변경사항/규칙을 가져오고
2. (호스트 LLM이) 리뷰를 생성하고
3. 결과를 저장/조회
4. **리뷰 결과를 Task로 변환하여 체계적으로 수정**  
   하는 흐름에 최적화되어 있습니다.

---

## 📋 규칙 파일 관리 (중앙 집중식)

**FE 리드**가 MCP 서버의 `templates/rules.ko.md` 파일을 직접 관리합니다.

```
mrt-fe-review-washer/
└── templates/
    └── rules.ko.md   ← FE 리드가 관리하는 "원본"
```

- 모든 프로젝트가 이 **하나의 규칙 파일**을 공유
- 규칙 변경 시 MCP 서버만 업데이트하면 모든 프로젝트에 즉시 반영
- `rules://active` 리소스로 현재 적용 중인 규칙 확인 가능

---

## 📋 기본 워크플로우 (리뷰 생성)

### 1. 리뷰 대상 diff 수집

```
tool: review.collect_diff
  - base: "main" (기본값)
  - head: "HEAD" (기본값)
```

### 2. 리뷰 프롬프트 패키지 만들기

```
tool: review.make_prompt
```

결과 텍스트를 그대로 모델에게 전달해서 리뷰를 작성하도록 합니다.
모델 출력은 `review.save`에 넣기 좋게 JSON 스키마를 포함합니다.

### 3. 리뷰 저장

```
tool: review.save
```

### 4. 리뷰 확인

```
tool: review.list
tool: review.get
tool: review.export_markdown
```

---

## 🎯 Task 관리 워크플로우 (리뷰 → 수정)

리뷰 결과의 각 finding을 **실행 가능한 Task로 변환**하여 체계적으로 관리합니다.

### 1. 리뷰 결과를 Task로 변환

```
tool: task.from_review
  - review_id: "rev_2026-01-18T..."
```

리뷰의 모든 findings가 개별 Task로 생성됩니다.

### 2. Task 목록 확인

```
tool: task.list
  - status: "pending" | "in_progress" | "completed" | "cancelled" (선택)
  - limit: 20 (기본값)
```

### 3. Task 실행 시작

```
tool: task.execute
  - id: "task_2026-01-18T..."
```

- 상태가 `in_progress`로 변경됩니다
- 실행 가이드(수정 방법)가 제공됩니다

### 4. 코드 수정

Task 가이드를 참고하여 코드를 수정합니다.

### 5. Task 검증

```
tool: task.verify
  - id: "task_2026-01-18T..."
```

검증 체크리스트를 확인합니다.

### 6. Task 완료

```
tool: task.complete
  - id: "task_2026-01-18T..."
  - verification_note: "수정 내용 메모" (선택)
```

### 7. 다음 Task 실행

Task 목록에서 다음 pending Task를 실행합니다.

---

## 📊 Task 상태 흐름

```
pending → in_progress → completed
    ↓           ↓
cancelled   cancelled
```

| 상태           | 설명                         |
| -------------- | ---------------------------- |
| ⏳ pending     | 대기 중 (아직 시작하지 않음) |
| 🔄 in_progress | 진행 중 (현재 작업 중)       |
| ✅ completed   | 완료됨                       |
| ❌ cancelled   | 취소됨                       |

---

## 🛠️ 전체 Tool 목록

### Review Tools

| Tool                     | 설명                      |
| ------------------------ | ------------------------- |
| `review.collect_diff`    | git diff 수집             |
| `review.make_prompt`     | 리뷰 프롬프트 패키지 생성 |
| `review.save`            | 리뷰 저장                 |
| `review.list`            | 리뷰 목록                 |
| `review.get`             | 리뷰 상세 조회            |
| `review.export_markdown` | 마크다운 변환             |
| `review.debug.env`       | 환경 변수 확인            |

### Task Tools

| Tool                 | 설명                      |
| -------------------- | ------------------------- |
| `task.from_review`   | 리뷰 findings → Task 변환 |
| `task.create`        | Task 수동 생성            |
| `task.list`          | Task 목록 (상태별 필터)   |
| `task.get`           | Task 상세 조회            |
| `task.execute`       | Task 실행 시작            |
| `task.verify`        | Task 검증                 |
| `task.complete`      | Task 완료                 |
| `task.delete`        | Task 삭제                 |
| `task.update_status` | Task 상태 변경            |
| `task.stats`         | Task 통계                 |

---

## 📚 Resources

| Resource              | 설명                    |
| --------------------- | ----------------------- |
| `rules://active`      | 활성 규칙 파일          |
| `reviews://index`     | 리뷰 인덱스 (최신 50개) |
| `reviews://latest`    | 최신 리뷰 상세          |
| `tasks://index`       | Task 인덱스 (최신 50개) |
| `tasks://pending`     | 대기 중인 Task          |
| `tasks://in_progress` | 진행 중인 Task          |

---

## 💬 Prompts

| Prompt         | 설명                    |
| -------------- | ----------------------- |
| `review`       | 코드 리뷰 템플릿      |
| `task-execute` | Task 실행 가이드        |
| `task-plan`    | Task 계획 수립          |
